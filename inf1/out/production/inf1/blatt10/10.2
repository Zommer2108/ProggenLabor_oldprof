1. Eine Referenz auf das aktuelle Objekt soll als Rückgabewert
   zurückgegeben werden.

2. Weil sie final sind und damit eine Konstante

3. Es wird ein neuer Punkt erstellt bei dem die x-Koordinate die negative y-Koordinate ist und die Y-Koordinate die x-Koordinate ist.

4. Es wird ein neuer Punkt "midPoint" und Line2D "ret" erstellt mit den Punkten "p1" und "p2".
   p1 und p2 werden dadurch definiert, das zuerst der alte midpoint mit dem neuen Midpoint der sich um 90° gedreht wurde addiert wird.
   Die entstehende Linie ergibt sich aus der Differenz der beiden Punkte.

   -> Eine neue Linie wird gezeichnet dessen Punkte sich um 90° verschoben wurden.

5. der erste Punkt kommt vom letzt erstellten Punkt

6.
    1. Method-Area

    2. Da die Referenz kopiert wird
